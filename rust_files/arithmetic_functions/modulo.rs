fn main() {
    let num1 = 31; // First number
    let num2 = 20; // Second number
    let modulo = num1 % num2; // Modulo

    println!("The modulo of {} and {} is {}", num1, num2, modulo);
}
