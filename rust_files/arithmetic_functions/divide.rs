fn main() 
{
    let num1: f64 = 40.674; // First number as a float
    let num2: f64 = 20.358; // Second number as a float
    let quotient = num1 / num2; // Division

    println!("The quotient of {:.3} divided by {:.3} is {:.3}", num1, num2, quotient);
}
