fn main() {
    let num1 = 40; // First number
    let num2 = 20; // Second number
    let remainder = num1 - num2; // Subtraction

    println!("The remainder of {} minus {} is {}", num1, num2, remainder);
}
